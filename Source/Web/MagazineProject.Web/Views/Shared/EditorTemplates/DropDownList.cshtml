@model int

<div class="row">
    <div class="col-md-2 text-primary">
        @Html.LabelFor(m => m)
    </div>
    <div class="col-md-3">
        @Html.DropDownListFor(m => m, (IEnumerable<SelectListItem>)ViewBag.ListItem, "Select...", new { @class = "form-control" })
    </div>
</div>
<br />
